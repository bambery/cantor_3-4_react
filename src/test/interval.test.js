import Interval from '../models/interval'
import Fraction from '../models/fraction'
import { IntervalRangeError } from '../shared/errors'

describe('Interval', function() {
    describe('creating intervals', function(){
        it('creates an interval when passed two appropriate fractions', function() {
            let left = new Fraction(0, 2)
            let right = new Fraction(1, 1)
        })
    })

})
