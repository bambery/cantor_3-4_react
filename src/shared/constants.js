// this is arbitrary - should determine a sensible limit
export const maxSegments = 50
export const minSegments = 3
export const maxIter = 50
export const stateDefaults = {
    'numSegments':  4,
    'toRemove':     [3],
    'numIter':      1
}
