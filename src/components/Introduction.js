import React from 'react'
import PropTypes from 'prop-types'

const Introduction = (props) => {
    return(
        <div className="centering-div">
            This site allows you to construct both symmetric and asymmetric Cantor-like sets and then download the results.
        </div>
    )
    /*
    return(
        <div className="centering-div">
            <div className="introduction">
                <div className="introduction-inner">
                    This site allows you to construct both symmetric and asymmetric Cantor-like sets and then download the results. To construct your set, first choose how many segments
                </div>
            </div>
        </div>
    )
    */
}

export default Introduction
